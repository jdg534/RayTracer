// rember render to be it's own file (not a class), for now

// look into the profiling, (compiler optimisations)

// after find way the have multi threads used to render a frame instead of multiple frames done per thread

rember trying to do realtime rendering more important


PS4 free BSD based

C++  "/" for folder notation works on windows
could use #ifdef _WINDOWS_DEF_HERE_
