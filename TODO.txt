// rember render to be it's own file (not a class), for now

// look into the profiling, (compiler optimisations)


port to PS4

implerment the delete ppm files in folder function via FIOS

next see what can be done using lib fiber for rendering a frame with multiple fibers

optionally with multiple fibers for multerple frames



PS4 free BSD based

C++  "/" for folder notation works on windows
could use #ifdef _WINDOWS_DEF_HERE_
